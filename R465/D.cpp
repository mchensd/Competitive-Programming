#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef pair<int, int> pii;

#define mp make_pair
#define pb push_back

vector<pii> states[100005];
int s1[100005], s2[100005];
int main() {
    #ifdef USE_INPUT_FILE
    freopen("D.in", "r", stdin);
    #endif
    ios::sync_with_stdio(0);
    int N, M; cin >> N >> M;
    for (int i=0; i<N; ++i) {
    	cin >> s1[i];
    }
    for (int i=0; i<N; ++i ){
    	cin >> s2[i];
    }
    ll P, Q;
    for (int i=0; i < N; ++i ){
    	
    }
}
